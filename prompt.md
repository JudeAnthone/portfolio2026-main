FRONTEND:
Code Quality Standards that you must follow for the frontend and admin-frontend, every time and every code that you will provide:
- Industry-grade React + TypeScript code.
- Not over-engineered: Simple, clean, and does the job for a specific component, page, hook, or utility.
- Well-commented: Explain the purpose of each component, function, hook, or block. Describe why the logic exists in a way that helps a beginner understand it. Not too long, just clear.
- Use TypeScript best practices: Proper interfaces, types, props typing, state typing, API response typing, and type safety throughout.
- Consistent error handling: Show proper user feedback for errors or empty states.
- API integration: Use axios or fetch with typed responses. Handle errors consistently. Prevent misuse of state or unauthorized actions. (Just remember this in the future use, do not yet put this in code since i dont have a plan in integrating backend from it)
- State management and context: Use React Context properly. State and actions must be clearly named and meaningful.
- Routing: Use routing perfectly
- UI/UX consistency: Follow the design system (colors, fonts, spacing, themes). Components should be reusable, modular, and responsive.
- Component structure and naming: File, folder, and component names should be meaningful and reflect their purpose. Components should have a single responsibility.
- Accessibility: Use semantic HTML, keyboard navigation, and ARIA attributes where necessary.
- I need you to state file names/folder on every file, code changes, and folders. so that i know what file im working on. 